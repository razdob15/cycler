<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/extra_time_label_margin">

        <!-- Search Line -->
        <RelativeLayout
            android:id="@+id/relLayout1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <include layout="@layout/snippet_inst_search_bar" />
        </RelativeLayout>

        <!-- List View -->
        <ListView
            android:id="@+id/list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/relLayout1" />


    </RelativeLayout>

    <Button
        android:id="@+id/add_new_fav_btn1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/colorBeautyBlue"
        android:text="Add new place to favorites"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="55dp"
        android:paddingStart="@dimen/start_end_margin"
        android:paddingEnd="@dimen/start_end_margin"
        android:textSize="12sp"
        android:textStyle="bold"
        android:visibility="gone"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/add_new_fav_btn2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="60dp"
        android:layout_marginEnd="10dp"
        android:src="@drawable/ic_map"
        android:background="@color/cycler_green"
        android:layout_alignParentEnd="true"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/colorGrey"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="50dp"/>
    <include layout="@layout/layout_bottom_navigation_view"/>

</RelativeLayout>